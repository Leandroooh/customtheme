import type { NextConfig } from "next";

const nextConfig = {
  allowedDevOrigins: ['http://172.26.80.1:3000'], // ou qualquer IP que você esteja usando
}
module.exports = nextConfig
export default nextConfig;
